runtime: python311

instance_class: F2

entrypoint: gunicorn --bind :$PORT --workers 1 --threads 4 --timeout 120 app:app

env_variables:
  GEMINI_API_KEY: "your_gemini_api_key_here"

automatic_scaling:
  min_instances: 0
  max_instances: 2
  target_cpu_utilization: 0.75
